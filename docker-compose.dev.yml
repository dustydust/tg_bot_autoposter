# Local development: docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
services:
  bot:
    build: .
    env_file: .env
    command: ["sh", "-c", "watchfiles --filter python 'python -m bot.main' /app/bot"]
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
    volumes:
      - ./data:/app/data
      - ./bot:/app/bot
    restart: "no"
    environment:
      - PYTHONUNBUFFERED=1
